<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" type="text/css" href="/styles.css">
    <title>Members Only</title>
  </head>
  <body>
    <%- include("./partials/navbar.ejs") %> 
    <div class="content">
    <% if (currentUser) {%>
      <div class="index-top-section">
          <%  if (currentUser.club_id) {%>
            <h1 class="greeting">Create a new post</h1>
            <%- include("./partials/createPost.ejs") %>
          <% } else {%>
            <p>Please join a <a href="/clubs">club</a> to create your first post!</p>
            <% } %>
        </div>    
        <h1 class="greeting">Most Recent Posts</h1>
      <%- include("./partials/loadAllPosts") %>
    <% } else {%>
            <h2 class="greeting">Please <a href="login" title="log in">login</a> to add a post</h2>
            <div class="divider"></div>
            <%- include("./partials/loadAllPosts") %>
    <% } %>
    </div>
    <script src="https://unpkg.com/lucide@latest"></script>
    <script>
      lucide.createIcons();
    </script>
  </body>
</html>
